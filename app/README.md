# Milestone 7 - Smart Product Assistant

Streamlit-based integration of all six milestones (M1-M6).

## Run locally
```bash
pip install -r ../requirements.txt
export GROQ_API_KEY=...
streamlit run app.py
```

All heavy work is delegated to `src/`. This file only wires up the UI.
